# Chave de autenticação do app para a API de licença.
# Copiada em tempo de build pelo build.rs via option_env!("AX_API_KEY").
# Gere com: php -r "echo bin2hex(random_bytes(32));"
# Em produção, setar como variável de ambiente no CI/CD (não commitar o .env).
AX_API_KEY=
